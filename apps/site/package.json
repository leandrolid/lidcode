{
  "name": "site",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "serve --cors public -p 8081",
    "build": "echo \"No build step required\"",
    "start": "pm2 serve public $PORT --no-daemon",
    "start:dev": "pm2 serve public 8081 --no-daemon",
    "heroku-postbuild": "npm run build"
  },
  "dependencies": {
    "pm2": "^6.0.14"
  },
  "devDependencies": {
    "serve": "14.2.5"
  }
}
